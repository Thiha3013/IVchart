# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Secrets
.env

# PyInstaller build output
build/
dist/
*.spec

# Data (large / regenerable)
*.parquet
# ...except stored chain snapshots, which are the dataset itself
!data/chains/**/*.parquet
# ...and the vendor dataset (9 MB), so deploys and CI get AAPL's 2021-23 history
!data/vendor/aapl_2021_2023.parquet
.vercel
.env*
