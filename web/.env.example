# Base URL of the API server, no trailing slash. Leave empty in local dev
# (Vite proxies /api to 127.0.0.1:8000). Set in Vercel project settings for prod.
VITE_API_BASE=https://your-api-host.example.com
